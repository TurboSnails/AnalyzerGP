# ═══════════════════════════════════════════════════════════════
# ai_app1 环境变量模板
# 复制为 .env 并填入真实值：
#     cp ai_app1/.env.example ai_app1/.env
#
# 说明：
#   • ai_app2 / ai_app3 / rag_framework 会自动回退读取 ai_app1/.env
#   • 所有以 RAG_ 为前缀的变量优先级高于兼容旧命名
# ═══════════════════════════════════════════════════════════════

# ── LLM API（必填）─────────────────────────────────────────────
# 实际使用 MiniMax API（OpenAI 兼容格式）
OPENAI_API_KEY=your_minimax_key

# 或通过 RAG_ 前缀精确控制（优先级更高）
# RAG_LLM_API_KEY=your_minimax_key
# RAG_LLM_BACKEND=minimax        # minimax | ollama | openai | local
# RAG_LLM_BASE_URL=https://api.minimaxi.com/v1
# RAG_LLM_MODEL=MiniMax-M2.7
# RAG_LLM_MAX_TOKENS=512

# ── 改写专用 LLM（可选，默认复用主 LLM）──────────────────────────
# 将查询改写/意图分析/查询分解等轻量任务分离到低成本模型（如本地 Qwen）。
# 不填写时自动复用上方主 LLM 配置。
# RAG_REWRITER_LLM_BACKEND=local          # minimax | ollama | openai | local
# RAG_REWRITER_LLM_MODEL=qwen2.5-1.5b-instruct
# RAG_REWRITER_LLM_BASE_URL=              # local/ollama 留空，minimax/openai 填端点
# RAG_REWRITER_LLM_API_KEY=               # 复用主 LLM Key 时可留空
# RAG_REWRITER_LLM_MAX_TOKENS=128

# ── 向量库与 BM25（通常无需修改，默认值已指向 ai_app1/data/）────
# 仅当需要把索引放到其他位置时才取消注释并修改：
# RAG_CHROMA_DB_PATH=ai_app1/data/chroma_db
# RAG_BM25_INDEX_DIR=ai_app1/data/tantivy_bm25

# ── HuggingFace（下载模型用，可选）──────────────────────────────
# 需要下载私有/门控模型时填写；公开模型留空即可
# HF_TOKEN=your_huggingface_token

# 中国大陆镜像（脚本默认自动走 hf-mirror）
# 要直连官方 Hub：设为空值或删除此行
# HF_ENDPOINT=https://hf-mirror.com

# ── 可选：下载脚本调优 ─────────────────────────────────────────
# HF_MAX_WORKERS=4
# HF_HUB_ETAG_TIMEOUT=30
# HF_DISK_STATS_INTERVAL=60
# HF_DOWNLOAD_RETRIES=5
# HF_DOWNLOAD_UNLOCK=0
# HF_IMPLICIT_TOKEN=0
# QWEN_REWRITER_REPO_ID=Qwen/Qwen2.5-1.5B-Instruct
